[package]
name = "todo_app"
version = "0.1.0"
edition = "2027.1"
description = "Minimal todo app demo for the Orison bootstrap toolchain"
license = "Apache-2.0"

[capabilities]
declared = ["http", "db.read", "db.write", "log"]

[scripts]
check = "ori check --json src/api.ori"
test = "ori test examples/todo_app"
